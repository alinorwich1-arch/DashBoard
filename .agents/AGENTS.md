# Project Guidelines

## Automatic GitHub Synchronization
- Whenever any files (code, requirements, configs) are modified or updated in this repository, ALWAYS commit and push the changes directly to GitHub (`origin main`) using `git add`, `git commit`, and `git push origin main`.
- Ensure the live Streamlit Cloud deployment (`https://dashboard-ali.streamlit.app/`) receives the updates automatically on every change.
